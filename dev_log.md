# 📋 Журнал разработки Kwasik Bot (dev_log)

В данном документе зафиксированы все улучшения и новые возможности, реализованные в ходе работы над Twitch RPG-ботом **Kwasik Bot**.

---

## 🚀 1. Архитектура и Производительность (Асинхронная БД) `[31.07.2026]`
* **Переход на `aiosqlite`:** Синхронные вызовы `sqlite3` были полностью заменены на асинхронные через библиотеку `aiosqlite`. Это исключило блокирование цикла событий `asyncio` при обращении к базе данных во время обработки чата в реальном времени.
* **Автоматические миграции базы данных:** В метод `DBManager.init_db()` добавлена проверка схемы таблицы `players` (`PRAGMA table_info`), позволяющая автоматически добавлять новые колонки (`title`, `achievements`, `pvp_wins`) при запуске бота на существующих базах данных без потери пользовательских данных.

---

## 🏆 2. Система достижений и Титулов `[31.07.2026]`
* **Достижения (`ACHIEVEMENTS`):**
  * `first_hunt` («Первая кровь») — за первую успешную охоту (+100 💰).
  * `hunter_10` («Опытный охотник») — за достижение 10 уровня (+500 💰).
  * `boss_slayer` («Убийца боссов») — за победу над боссом в подземелье (+1000 💰).
  * `pvp_winner` («Дуэлянт») — за победу в PvP-дуэли (+300 💰).
* **Титулы (`TITLES` с бонусами к характеристикам):**
  * `novice` («Новичок») — без бонусов.
  * `hunter_e` («Охотник E-ранга») — бонусы к силе и живучести (+5 STR, +5 VIT). Требует достижение `hunter_10`.
  * `shadow_monarch` («Теневой Монарх») — мощные бонусы к характеристикам (+25 STR, +15 AGI, +20 VIT). Требует достижение `boss_slayer`.
* **Интеграция в движок (`RPGEngine`):**
  * Метод `get_stats(p)` теперь автоматически учитывает бонусы от экипировки и активного титула.
  * Метод `unlock_achievement(p, ach_id)` проверяет, получено ли достижение, начисляет награду в золоте и обновляет данные игрока.

---

## 🤝 3. Торговля и Обмен между игроками `[31.07.2026]`
* **Команда `!передать`:**
  * `!передать золото @юзер <сумма>` — переводит золото от одного зрителя к другому с проверкой баланса.
  * `!передать предмет @юзер <название>` — переносит указанный предмет из инвентаря отправителя в инвентарь получателя.

---

## 👥 4. Пати и Совместные подземелья (Co-op Dungeons) `[31.07.2026]`
* **Система групп (`!пати`):**
  * `!пати создать` / `!пати инвайт` / `!пати принять` / `!пати покинуть` — управление группой до 5 человек.
  * `!пати ход <ID>` — перемещение всей группы во врата определенного ранга.
  * `!пати охота` — совместный рейд группы на монстров и боссов подземелий с общим уроном и распределением наград среди живых участников.

---

## ⚔️ 5. Пошаговые PvP-дуэли и Рейтинг `[31.07.2026]`
* **Пошаговая боевая система (до 9 раундов):**
  * Дуэли между игроками (`!дуэль @юзер` -> `!принять`) теперь проходят по раундам с детальным логом.
  * **Механика характеристик:** Ловкость (`agi`) отвечает за шанс уклонения и криты, восприятие (`sen`) контрит уклонение противника, выносливость (`vit`) снижает входящий урон.
* **Рейтинг и достижения:**
  * Отслеживание побед (`pvp_wins`) и таблица лидеров по команде `!топ_пвп`.
  * Разблокировка достижения «Дуэлянт» (`pvp_winner`) при победе в дуэли.

---

## 🎒 6. Расширение ассортимента предметов и лута `[31.07.2026]`
* **Новые расходники и экипировка:**
  * Большие зелья здоровья (`greater_hp_potion`) и маны (`greater_mp_potion`).
  * Новое оружие: Рыцарский меч (`knight_sword`), Рыцарское копьё (`knight_spear`), Демонический кинжал (`demon_dagger`).
  * Новая броня: Стальная броня (`steel_armor`), Плащ тени (`shadow_cloak`).
  * Новые аксессуары: Кольцо силы (`ring_of_power`), Амулет мудрости (`amulet_of_wisdom`).
* **Обновление дропа в вратах (Ранги C — S):** Боссы и мобы в средних и высоких подземельях теперь выбивают новое элитное снаряжение.

---

## 🎮 7. Новые Twitch-команды `[31.07.2026]`
* `!достижения` — выводит список всех разблокированных достижений игрока.
* `!титулы` — выводит список доступных титулов, их требования и текущий активный титул.
* `!титул <ключ>` — позволяет сменить активный титул персонажа (при выполнении условий разблокировки).
* `!передать золото/предмет` — система обмена между зрителями стрима.
* `!пати` (и подкоманды) — управление пати и совместные походы в подземелья.
* `!дуэль` / `!принять` — пошаговые PvP-дуэли.
* `!топ_пвп` — таблица лидеров дуэлянтов.
* Обновлена команда `!статы` (отображает активный титул) и `!команды` (добавлены все новые команды).

---

## 🧪 8. Тестирование и Качество кода `[31.07.2026]`
* **Юнит-тесты (`tests/test_engine.py`, `tests/test_pvp.py`):** Написаны и успешно пройдены тесты для проверки формул характеристик, максимального HP/MP, рангов, повышения уровня, системы достижений, титулов и PvP-побед.
* **Линтинг и форматирование (`ruff`):** Весь код проверен и отформатирован по стандартам `ruff`, соблюдена строгая асинхронность и типизация Python 3.11+.

---

## 🔮 9. Улучшенная магическая система `[01.08.2026]`
* **Новые заклинания (`SPELLS`):**
  * `метеор` («Метеорит») — мощный атакующий спелл (90 MP, множитель урона `x12.0`).
  * `хил` («Исцеление») — исцеляющее заклинание (40 MP, восстанавливает HP в зависимости от интеллекта).
  * `щит` («Магический щит») — защитное заклинание (35 MP, создает магический щит / барьер).
* **Пассивная регенерация маны:**
  * В фоновый цикл регенерации (`_passive_regen_logic`) добавлено восстановление маны каждые 3 минуты, масштабирующееся от характеристики **Интеллект (`int_stat`)**.
* **Баланс и криты магии:**
  * Унифицирован шанс магического критического удара (зависит от **Восприятия / `sen`**).
  * Поддержка лечения и щитов в подземеллях (`!охота каст`) и рейдовых боссах (`!рейд каст`).

---

## 🛡️ 10. Отображение экипировки в профиле и реализация недостающих команд `[01.08.2026]`
* **Экипировка в профиле (`!статы` / `!профиль`):**
  * Теперь при запросе профиля охотника на экран выводятся названия надетых предметов в слотах Оружия, Брони и Аксессуара (или «Пусто», если слот свободен).
* **Реализация недостающих команд:**
  * `!профиль` и `!помощь` (алиасы к `!статы` и `!команды`).
  * `!отдых` (полное восстановление HP и MP).
  * `!продать <предмет>` (продажа предметов из инвентаря в магазин за половину стоимости).
  * `!отклонить` (отклонение вызова на дуэль).
  * `!атака` (быстрая атака в рейде).
* **Стабильность бота:**
  * Добавлена безопасная обработка исключений при отправке несуществующих команд в чат Twitch, предотвращающая падения бота из-за `CommandNotFound`.

---

## 🚨 11. Скрытые Красные Врата и Система Крафта с Кристаллами `[01.08.2026]`
* **Красные Врата (Случайные Ивенты):**
  * Автоматический фоновый спавн Красных Врат (S-ранга) с боссом на 100,000 HP.
  * Уведомление в Twitch-чат и 3 минуты на участие по команде `!войти`.
  * Автоматический расчет общего урона отряда: при победе выдается золото, опыт и редкие кристаллы (A/S).
* **Магические Кристаллы и Крафт:**
  * Добавлены кристаллы рангов от E до S, выпадающие при охоте в подземельях и зачистке врат.
  * Добавлены рецепты крафта (`!крафты` / `!крафт <предмет>`) для создания уникального элитного снаряжения (Кинжал Тени, Перстень Монарха, Клинок Дракона, Эликсир Жизни).

---

## ⚔️ 12. Пошаговые тактические PvP-дуэли `[01.08.2026]`
* **Пошаговая одновременная боевая система:**
  * После вызова (`!дуэль`) и принятия (`!принять`) дуэль переходит в интерактивный пошаговый режим.
  * Каждый раунд участники одновременно вводят свои действия в чат:
    * `!бой удар` — физическая атака (сила + ловкость).
    * `!бой каст <шар/молния/метеор/хил/щит>` — магическая атака, лечение или создание магического щита с расходом маны (`MP`).
  * **Инициатива:** Рассчитывается в каждом раунде по формуле: $\text{AGI} \times 1.0 + \text{SEN} \times 0.8 + \text{rng}(0, 5)$. Это позволяет магам и сенсорам с высоким восприятием на равных соревноваться с чистыми ловкачами.
   * Бой длится до победы одного из игроков или до 10 раундов.

---

## 📅 13. Ежедневные квесты и Таблица лидеров по золоту `[02.08.2026]`
* **Ежедневный квест Системы (`!квест` / `!награда`):**
  * Динамическая генерация ежедневного квеста охоты в зависимости от уровня и ранга игрока (цель: уничтожить 3 монстров в соответствующем подземелье).
  * Автоматический трекинг прогресса при успешной охоте (`!охота`).
  * Награда при завершении (`!награда`): `+3 AP`, `+50 EXP`, золото (`lvl * 100`), полное восстановление HP и MP и шанс повышения уровня (доступно 1 раз в сутки).
* **Таблица лидеров богачей (`!топ_золото`):**
  * Отображение топ-5 игроков стрима по количеству накопленного золота.

---

## 🌐 14. Совместимость с Render.com (Web Service Port Binding) `[30.08.2026]`
* **Встроенный HTTP Health-check сервер:**
  * Добавлен легковесный фоновый HTTP-сервер на базе стандартной библиотеки (`http.server` и `threading`), который слушает порт из переменной окружения `PORT` (или 10000 по умолчанию).
  * Это позволяет хостингам типа Render.com успешно фиксировать открытый порт для Web Service и проходить health-check проверки, в то время как бот стабильно функционирует в Twitch-чате.

---

## 📂 15. Команда ссылки на репозиторий `[!гит / !git]` `[30.08.2026]`
* **Ссылка на исходный код:**
  * Добавлены команды `!гит` и `!git`, позволяющие зрителям стрима быстро получить ссылку на GitHub-репозиторий бота (`https://github.com/WastleQ/kwasik-bot/tree/main`).

---

## 🗄️ 16. Поддержка PostgreSQL (Облачная База Данных на Render) `[30.08.2026]`
* **Гибридный менеджер БД (`DBManager`):**
  * Добавлена поддержка **PostgreSQL** (через асинхронную библиотеку `asyncpg`) при наличии переменной окружения `DATABASE_URL` (предоставляемой Render).
  * Сохранена обратная совместимость с **SQLite** (`aiosqlite`) при отсутствии `DATABASE_URL` для локального запуска и прогона тестов `pytest`.
  * Теперь прогресс игроков не сбрасывается при деплоях и перезапусках бота на Render.

---

## ⏱️ 17. Индивидуальный кулдаун команд для чаттеров `[30.08.2026]`
* **Ограничение частоты запросов (Cooldown):**
  * Добавлен персональный кулдаун 5 секунд для каждого зрителя в чате на выполнение команд бота.
  * Администраторы бота освобождены от кулдауна для оперативного управления.

---

## 💎 18. Компактное отображение находок при охоте `[30.08.2026]`
* **Группировка лута в чате:**
  * Если при охоте выпадает 1 предмет, выводится `💎 Находка: [Предмет]!`.
  * Если выпадает несколько предметов, они группируются через запятую: `💎 Находки: [Предмет 1], [Предмет 2]!`, что существенно разгружает чат стрима от лишнего текста.

---

## 🔮 19. Исправление ошибки в команде `!заклинания` `[31.08.2026]`
* **Обработка неатакующих заклинаний:**
  * Исправлен `KeyError: 'damage_mult'` при вызове команды `!заклинания`.
  * Теперь для лечащих (`хил`) и защитных (`щит`) заклинаний корректно выводятся параметры восстановления HP и прочности щита вместо множителя урона.

---

## 🏗️ 20. Архитектурный рефакторинг и доработка кода `[31.08.2026]`
* **Модуль конфигурации (`src/config.py`):**
  * Параметры окружения, токены и список администраторов вынесены из точки входа в отдельный файл конфигурации.
* **Пул соединений PostgreSQL (`asyncpg.create_pool`):**
  * `DBManager` оптимизирован для использования пула соединений при работе с PostgreSQL, что повышает производительность при высокой активности в чате.
* **Структурированное логирование (`logging`):**
  * Заменены все вызовы `print()` на стандартный модуль `logging` с настраиваемыми уровнями и форматом вывода.

---

## 👑 21. Улучшение команды `!титулы` и гибкий выбор `!титул` `[31.08.2026]`
* **Компактный вывод `!титулы`:**
  * Убрано слово «Доступные», технические ключи в квадратных скобках и лишний текст требований (теперь для доступных титулов пишется статус «🔓 Доступен», а заблокированные выводятся без текста требований).
* **Гибкий поиск по названию для `!титул`:**
  * Команда `!титул` теперь поддерживает регистронезависимый ввод на русском и английском языках, без учета пробелов, дефисов и подчеркиваний (например: `!титул новичок`, `!титул Охотник Е-ранга`, `!титул теневой монарх`).

---

## 🏪 22. Категоризированный и компактный вывод магазина (`!магазин`) `[31.08.2026]`
* **Разделение товаров по категориям:**
  * Команда `!магазин` теперь структурирует ассортимент по категориям (🧪 Расходники, ⚔️ Экипировка, 💎 Кристаллы), избавляя чат от громоздкой монолитной строки.

---

## 🛠️ 23. Исправление багов торговли, характеристик и HP `[31.08.2026]`
* **Пакетная продажа и снятие экипировки (`!продать`):**
  * Добавлена поддержка указания количества при продаже (например, `!продать Кристалл D-ранга 3`).
  * Если игрок продает надетый предмет и в инвентаре больше не остается копий этого предмета, слот автоматически очищается (`unequip`).
* **Упрощение отображения кристаллов в магазине:**
  * Кристаллы теперь отображаются лаконично: `E (50💰) | D (100💰) | C (250💰) | B (500💰) | A (1000💰) | S (2500💰)`.
* **Исправление прокачки восприятия (`sen` / `восприятие`):**
  * Добавлена поддержка характеристики `sen` в команде распределения очков `!кач` / `!прокачка`.
* **Защита HP и корректный пересчет при смене экипировки/дуэлях:**
  * Текущее HP теперь автоматически ограничивается сверху при смене снаряжения, если максимальное здоровье уменьшилось.
  * После завершения PvP-дуэлей побежденные игроки больше не уходят в отрицательное HP (теперь срабатывает стандартная механика `handle_death`).

---

## 🛡️ 24. Защита от превышения лимита Twitch в 500 символов для инвентаря `[31.08.2026]`
* **Ограничение длины сообщения `!инвентарь`:**
  * Добавлена проверка длины сообщения инвентаря. Если список предметов превышает 460 символов (например, при переполнении дубликатами от админ-команд), текст аккуратно обрезается с добавлением маркера `... (и др.)`, предотвращая ошибку Twitch API `Content must not exceed 500 characters`.

---

## ⚖️ 25. Балансировка боя, магии и отображение HP после охоты `[31.08.2026]`
* **Нерф имбалансной магии и бафф физического урона:**
  * Снижены множители урона заклинаний (Шар: `3.0`, Молния: `5.0`, Метеор: `7.5`) и увеличены их затраты по мане (MP).
  * Снижен множитель «Сферы Алчности» с `2.0` до `1.3`.
  * Увеличен множитель физической атаки (`str * 3.0 + agi * 0.8`), чтобы ближний бой и щиты были конкурентоспособными.
* **Отображение оставшегося HP после победы в подземелье:**
  * После завершения команды `!охота` теперь выводится текущее здоровье охотника (например: `(❤️ Осталось: 550/700 HP)`).
* **Расширенная документация (`README.md` и `wiki.md`):**
  * Добавлен полный список всех команд бота и подробное описание игровых механик.

---

## 💀 26. Новая механика смерти, отображение HP/MP и продажа лута с боссов `[31.08.2026]`
* **Штраф при смерти:**
  * При гибели в подземелье персонаж возрождается в городе, сохраняя **10% от макс. HP**, **10% от макс. MP**, но теряя **10% от текущего золота**.
* **Отображение HP и MP после охоты:**
  * Команда `!охота` теперь показывает остаток как здоровья, так и маны: `(❤️ Осталось: 100/100 HP, 🔮 100/100 MP)`.
* **Продажа любых предметов (`!продать`):**
  * Теперь можно продавать любые предметы из инвентаря (включая редкий лут и оружие с боссов, имевшие нулевую цену в магазине — они автоматически оцениваются в 250 золота за штуку).

---

## ⚔️ 27. Корректировка опыта и вывод урона после охоты `[31.08.2026]`
* **Балансировка получаемого опыта:**
  * Коэффициент получения опыта за победу над мобами и боссами снижен с `mob_hp * 0.4` до `mob_hp * 0.2`, предотвращая слишком быстрые скачки на десятки уровней за один бой.
* **Отображение нанесенного урона:**
  * После победы в подземелье бот теперь выводит точный объем нанесенного игроком урона в формате: `Победа над [моб]! Урон: [значение]. +[EXP] EXP...`.

---

## 🛡️ 28. Автоматическое ограничение HP/MP и отмена штрафа золота в PvP `[31.08.2026]`
* **Защита от превышения макс. HP/MP (`clamp_resources`):**
  * Текущие здоровье и мана автоматически ограничиваются сверху значением максимума (предотвращая баги вроде `700/625 HP`).
* **Отмена потери золота при поражении в PvP:**
  * При проигрыше в PvP-дуэлях здоровье снижается до 10%, но золото и локация охотника полностью сохраняются (штраф с потерей 10% золота и сбросом в город теперь применяется только при смерти в PvE-подземельях).

---

## 💬 29. Тегирование пользователей, команда `!правила` и улучшенный вывод критов `[31.08.2026]`
* **Тегирование пользователей в чате (`@username`):**
  * Команды `!охота` и `!кач` теперь явно тегают использующего их зрителя в начале сообщения (например: `@koenixq ⚔️ Победа над...`).
* **Новая команда `!правила` / `!rules`:**
  * Добавлена команда для быстрого вызова ссылки на подробный справочник и правила проекта (Wiki).
* **Компактный вывод критического урона магии:**
  * Убран отдельный текст с эмодзи в конце строки победы, теперь крит отображается лаконично: `Урон: 225 крит.`.
* **Очистка `README.md`:**
  * Из файла `README.md` удалена техническая инструкция по установке и быстрому старту, оставлены только описание проекта и полный список команд.

---

## ⚡ 30. Кулдаун на отдых (10 минут) и ребаланс магии/заклинаний `[31.08.2026]`
* **Кулдаун команды `!отдых`:**
  * Добавлено ограничение: восстанавливать здоровье и ману через `!отдых` теперь можно не чаще чем **раз в 10 минут**.
* **Ребаланс магических заклинаний:**
  * Существенно увеличены затраты маны (MP) и снижены избыточные множители урона для предотвращения бесконечного спама заклинаниями:
    * **Огненный шар (`шар`):** 50 MP, множитель урона `2.0`.
    * **Удар молнии (`молния`):** 100 MP, множитель урона `2.5`.
    * **Метеорит (`метеор`):** 300 MP, множитель урона `3.0`.
  * Это делает зелья маны (`mp_potion`) и менеджмент ресурсов важной частью игрового процесса.

---

## 🔮 31. Магическая мета: скалирование критов от INT+SEN и игнорирование уклонения `[31.08.2026]`
* **Шанс магического критического удара:**
  * Шанс маг. крита теперь масштабируется от суммы интеллекта (`INT`) и восприятия (`SEN`), достигая до 45%.
* **Игнорирование уклонения мобов магией:**
  * Магические атаки теперь наносят урон гарантированно (обходя шанс уклонения ловких монстров), в то время как физические атаки по-прежнему подвержены уклонению мобов.

---

## 📊 32. Автоматическая двусторонняя нормализация характеристик при изменении уровня (`normalize_player_stats`) `[31.08.2026]`
* **Двусторонняя адаптация статов под новый уровень:**
  * Если уровень игрока уменьшается в БД, а статы превышают лимит, они сбрасываются к базе (10), а избыточные очки возвращаются в `stat_points`.
  * Если уровень игрока увеличивается или возвращается обратно (и суммарных очков меньше положенного по уровню), недостающие очки характеристик автоматически доначисляются в свободные `stat_points`.

---

## 📉 33. Дополнительный ребаланс магического урона `[31.08.2026]`
* **Снижение множителей урона заклинаний:**
  * **Огненный шар (`шар`):** множитель урона снижен с `2.0` до `1.3`.
  * **Удар молнии (`молния`):** множитель урона снижен с `2.5` до `1.7`.
  * **Метеорит (`метеор`):** множитель урона снижен с `3.0` до `2.2`.

---

## 🏗️ 34. Реструктуризация архитектуры и статических данных (`src/data/`) `[31.08.2026]`
* **Модульная структура данных:**
  * Большой файл `src/data.py` разделен на логические подмодули в пакете `src/data/`: `items.py`, `spells.py`, `dungeons.py`, `recipes_titles.py`.
  * Пакет `src/data/__init__.py` выступает в роли единого экспортирующего фасада для полной обратной совместимости.
* **TUI Утилита администратора (`admin_tui.py`):**
  * Создана интерактивная консольная утилита для управления базой данных игроков, изменения уровней с автоматическим двусторонним пересчетом характеристик (`normalize_player_stats`), золота и инвентаря.

---

---

## 💧 35. Ребаланс максимальной маны (MP) `[31.08.2026]`
* **Уменьшение прироста MP от интеллекта (`INT`):**
  * Множитель прироста маны за каждую единицу интеллекта уменьшен с `10` до `5` (`150 + INT * 5`).
  * Базовое значение (150 MP) сохранено. При загрузке игроков ресурсы автоматически пересчитываются и подстраиваются под новую формулу.

---

## 🩸 36. Ребаланс максимального здоровья (HP) `[31.08.2026]`
* **Уменьшение прироста HP от выносливости (`VIT`):**
  * Множитель прироста здоровья за каждую единицу выносливости уменьшен с `15` до `10` (`100 + VIT * 10`).
  * Базовое значение (100 HP) сохранено. При загрузке игроков здоровье автоматически пересчитывается под новую формулу.

---

## 📈 37. Ребаланс прокачки уровня (Уровневые тиры: х2 опыта каждые 100 уровней) `[31.08.2026]`
* **Многоуровневая прогрессия опыта:**
  * Уровни 1-99: стандартная формула (`lvl * 100`, множитель `1x`).
  * Уровни 100-199: множитель `2x` (`lvl * 100 * 2`).
  * Уровни 200-299: множитель `4x` (`lvl * 100 * 4`) и далее каждые 100 уровней требования удваиваются (`2 ** (lvl // 100)`).
  * Логика повышения уровня централизована в `RPGEngine.check_level_up` и используется везде (охота, рейды, ежедневные квесты).

---

## ⏱️ 38. Кулдаун 10 секунд на команду `!охота` `[31.08.2026]`
* **Защита от макросов и спама:**
  * Добавлен кулдаун в **10 секунд** на использование команды `!охота` для обычных зрителей.
  * Если игрок отправляет команду чаще чем раз в 10 секунд, бот **не отвечает ничего** (тихий игнор). Администраторы бота освобождены от кулдауна.

---

## 🛡️ 39. Полноценная работа Магического Щита (`shield`) `[01.09.2026]`
* **Механика щита:**
  * В модель игрока и базу данных добавлено поле `shield`.
  * Применение заклинания щита (`!каст щит` или `!охота каст щит`) добавляет прочность к текущему щиту игрока.
  * Во время охоты урон от мобов сначала поглощается щитом игрока, и лишь после его разрушения начинает наносить урон по обычному HP.
  * В профиле (`!профиль`) щит отображается как дополнительное здоровье (например, `HP: 150(+50)/300`).

---

## 👹 40. Возврат и тонкая настройка урона мобов в подземельях C–S рангов `[01.09.2026]`
* **Балансировка мобов:**
  * Урон мобов в подземельях C, B, A рангов возвращен к оригинальным значениям, а для S ранга скорректирован до `1500 – 2500`:
    * **Ранг C:** `70 – 120`
    * **Ранг B:** `200 – 350`
    * **Ранг A:** `500 – 900`
    * **Ранг S:** `1500 – 2500`

---

## 🏝️ 41. Ребаланс подземелья S-ранга (Остров Чеджу) `[02.09.2026]`
* **Адаптация S-ранга для игроков 50+ уровней:**
  * Здоровье обычного моба (Мутант-муравей) снижено со 100,000 до **50,000 HP**.
  * Здоровье босса (Король Муравьев) снижено с 400,000 до **100,000 HP**.
  * Урон мобов в S-ранге снижен до **1000 – 2000**, чтобы игроки 50+ уровней могли проходить локацию при грамотном использовании щитов и хила.
















